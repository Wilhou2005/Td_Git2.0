#include <stdio.h>

int main() {
    int x = 10;
    int y = 5;
    printf("x = %d\n", y);//'y' undeclared (first use in this function) y n'etait pas declarer donc ça ne pouvait pas marcher 
    return 0;
}
